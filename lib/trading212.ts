// Trading 212 REST client — read-only
// Wired up in PROMPT2. Placeholder exported so imports resolve.

export async function getPortfolio(): Promise<unknown> {
  throw new Error("T212 client not yet implemented — see PROMPT2");
}
