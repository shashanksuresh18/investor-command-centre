// Gmail client — read-only (gmail.readonly scope)
// Wired up in PROMPT2. Placeholder exported so imports resolve.

export async function fetchRecentEmails(): Promise<unknown[]> {
  throw new Error("Gmail client not yet implemented — see PROMPT2");
}
